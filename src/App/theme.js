import { createMuiTheme } from 'material-ui/styles';
import { blue } from 'material-ui/colors';

const theme = createMuiTheme({
  palette: {
    primary: blue // lighter than indgo
  }
});

export default theme;
